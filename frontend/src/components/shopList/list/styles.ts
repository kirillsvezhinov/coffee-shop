import styled from "styled-components";

import { Container } from "../../../styles/global";

const ListStyled = styled(Container)`
    padding: 22px 20px 22px 0;

    display: flex;
    flex-direction: column;
    gap: 30px;
`;

export { ListStyled };
